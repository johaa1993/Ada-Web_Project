with Ada.Strings.Fixed;
with Ada.Strings;
with Ada.Text_IO;
with Ada.Integer_Text_IO;
with HTTP;

procedure Test_Index is
   use Ada.Strings;
   use Ada.Strings.Fixed;
   use Ada.Text_IO;
   use Ada.Integer_Text_IO;

begin

   Put ("Index: ");
   --Put (Index (HTTP.Line_Content_Type, ": ", Backward));
   New_Line;


end;
